spring:
  application:
    name: ppt-agent-app

server:
  port: 8080

gateway:
  client:
    host: localhost
    port: 9090

# Rendered decks: GET /v1/ppt/download/{fileName}
ppt:
  output:
    dir: build/output/pptx
